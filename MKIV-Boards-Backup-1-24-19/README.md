# MKIV

```
$ sudo apt-get update
$ sudo apt-get upgrade
$ sudo add-apt-repository --yes ppa:js-reynaud/kicad-5
$ sudo apt-get update
$ sudo apt install kicad freecad gcc-avr binutils-avr gdb-avr avr-libc avrdude
```
